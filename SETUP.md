# Install Flutter

```python
git clone https://github.com/flutter/flutter.git
sudo ~/.zshrc (ctrl + X, ctrl + S; ctrl + X, ctrl + C)
export PATH="$PATH:/Users/luxin/Desktop/flutter/bin"
source ~/.zshrc
flutter --version
flutter doctor

# Authetication
ls -ld /Users/luxin/Desktop/Tools/flutter/.pub-preload-cache
chmod -R 755 /Users/luxin/Desktop/Tools/flutter
lsof | grep /Users/luxin/Desktop/Tools/flutter

```

# Steps

```python
cd Desktop
flutter create instagram_flutter
```
